# Sensitive files and credentials
.env
.env.local
.env.*.local
*.key
*.pem
*.crt
credentials.json
secrets.json
.credentials
config.local.*
.config.local
oauth_tokens.json

# IDE files
.vscode/
.idea/
*.swp
*~
.DS_Store

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.pytest_cache/

# Compiled extension module (built in-place by `maturin develop` into
# clusteraudiencekit/) and maturin/wheel build output
*.so
*.pyd
*.dylib
wheels/
target/wheels/

# Virtual environments
venv/
env/
.venv

# Rust
target/
# NOTE: Cargo.lock is intentionally tracked (not ignored) for reproducible
# `cargo build`/`maturin build` output across contributors and CI.

# Temp files
*.tmp
*.log
temp/
tmp/
